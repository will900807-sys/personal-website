# Codex 项目自定义指令

## 一、项目安全规则

### 禁止批量删除

在本目录及其子目录内，Codex 严禁执行任何批量删除文件或目录的操作。

禁止使用以下命令或等价写法：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

也禁止通过以下方式变相批量删除：

- 使用通配符删除，例如 `Remove-Item *.log`、`del *.tmp`
- 使用管道批量删除，例如 `Get-ChildItem ... | Remove-Item`
- 使用循环删除多个文件或目录
- 删除整个目录后重建
- 跨 shell 拼接删除命令，例如 PowerShell 枚举路径后交给 `cmd /c` 删除
- 通过脚本批量清理、批量移动到回收站或批量覆盖文件

如确实需要删除文件，只允许一次删除一个明确、完整、可核对的文件路径。

正确示例：

- `Remove-Item -LiteralPath "C:\path\to\file.txt"`

删除前必须确认该路径是单个文件，不是目录，不包含通配符，不是递归删除。

如果任务需要删除多个文件、清理目录、递归删除、按规则批量清理缓存或临时文件，Codex 必须停止操作，并告知用户需要手动处理，不能自行执行批量删除。

除非用户明确指定某一个空目录，并且不使用递归参数，否则不要删除目录。

## 二、AGENTS.md 固化规则

当进入一个项目目录工作时，Codex 应优先检查当前目录是否存在 `AGENTS.md`。

如果存在 `AGENTS.md`：

- 先阅读其中的项目规则、禁止事项、常用命令、资料入口、交付要求和历史决策；
- 后续操作必须遵守其中的约束；
- 如果用户的新要求与 `AGENTS.md` 冲突，应先提醒用户确认，不要擅自覆盖原规则；
- 如果发现 `AGENTS.md` 已过时，应说明依据，并建议更新。

如果不存在 `AGENTS.md`，且用户要求“通读项目”“继续这个项目”“沉淀规则”“下次接着做”“保存项目经验”“把项目情况记下来”等：

- 应在项目根目录创建 `AGENTS.md`；
- 内容使用中文；
- 结构应清晰，适合下次直接接手；
- 不要只在聊天里总结，应把可复用信息写入 `AGENTS.md`。

建议 `AGENTS.md` 至少包含以下内容：

- 项目简介
- 当前目标
- 重要路径
- 资料入口
- 常用命令
- 数据库、接口或业务口径
- 已确认规则
- 禁止操作
- 当前进展
- 待办事项
- 容易踩坑的问题
- 下次继续时优先查看的文件

在完成重要任务后，如果发现有长期复用价值的信息，应主动建议更新 `AGENTS.md`，或在用户明确允许时直接补充进去。

可写入 `AGENTS.md` 的内容包括：

- 项目入口文件
- 关键脚本和运行命令
- 数据来源和统计口径
- 接口地址、字段含义和调用限制
- 已确认的业务规则
- 用户明确强调的偏好或禁忌
- 反复出现的问题和解决办法
- 后续继续工作的建议

不得写入 `AGENTS.md` 的内容包括：

- 临时闲聊
- 一次性过程记录
- 未确认猜测
- 无证据结论
- 与项目无关的个人信息

## 三、工作方式规则

Codex 不要停留在泛泛建议。如果用户提出的是可执行任务，应主动检查文件、分析资料、编写或修改内容，并尽量交付可直接使用的结果。

在开始修改前，应先理解项目现状：

- 优先查看 `AGENTS.md`、`README`、目录说明、SOP、状态文件、核心脚本；
- 搜索文件时优先使用 `rg` 或 `rg --files`；
- 如果目录不是 Git 仓库，不要强行依赖 Git，应改用文件盘点和资料入口分析；
- 对中文路径、中文 Office 文件、中文 CSV/Excel，优先使用 UTF-8 安全方式处理。

如果工作涉及代码或脚本：

- 优先复用现有项目结构和风格；
- 不要引入不必要的新框架；
- 修改范围应尽量小；
- 不要重构无关代码；
- 不要覆盖用户已有改动；
- 修改后应尽量运行测试、脚本或最小验证命令。

如果工作涉及文档、制度、汇报、Word、Excel、PPT：

- 默认使用中文；
- 结构要正式、清晰、可直接交付；
- 对外或对上汇报口径应稳妥，不夸大，不写满；
- 对未确认事项使用“待确认”“建议核实”“根据现有资料判断”等审慎表述；
- 不要杜撰数据、人员、结论或审批状态。

## 四、验证和交付规则

完成任务前，Codex 应尽量做一次验证：

- 代码任务：运行测试、构建、脚本或最小复现；
- 数据任务：核对行数、字段、时间范围和统计口径；
- 文档任务：检查标题、目录、格式、关键结论和是否遗漏用户要求；
- 自动化任务：说明触发条件、输入、输出和失败风险。

如果无法验证，应明确说明原因，不要假装已经验证。

最终回复应简洁说明：

- 做了什么；
- 产物在哪里；
- 是否已验证；
- 还有哪些风险或后续建议。

## 五、用户偏好规则

默认使用中文回复。

用户更重视可落地结果，而不是空泛建议。能直接做的事情应直接做；需要用户确认的，只问关键问题。

当用户说“继续”“继续往下做”“你判断下”“帮我完成”时，默认主动推进下一步，不要只给待办清单。

当用户要求“通读当前文件夹”“把项目记下来”“沉淀到 AGENTS.md”时，默认产出或更新项目根目录下的中文 `AGENTS.md`。

当用户要求正式材料时，默认输出稳妥、正式、可转发的版本。

当用户指出前面遗漏或做错时，应先承认并修正，不要解释过多。

## 六、当前项目说明

### 项目简介

本项目是 will_station 的个人 AI 落地专家网站，定位为“will_station专注于AI应用落地”，面向中小企业老板、园区/招商/制造/服务型企业负责人，重点展示企业 AI 应用落地、流程自动化、内容获客、数据整理、知识库和轻量管理工具搭建能力。

### 当前目标

- 建设一个可直接通过 GitHub Pages 发布的纯静态单页网站。
- 第一版以微信作为主要联系入口，不再保留其他平台占位模块。
- 页面口径强调“先找业务场景，再做 AI 落地”，避免空泛宣传和虚构案例。

### 重要路径

- `index.html`：网站首页和主要中文文案。
- `styles.css`：页面样式和响应式布局。
- 当前首页主视觉由 `index.html` 和 `styles.css` 中的产品化工作台界面组成，不依赖外部主视觉图片。
- `README.md`：本地预览、联系方式替换和 GitHub Pages 发布说明。

### 常用命令

```powershell
python -m http.server 8080
git status --short --branch
git add -- index.html styles.css README.md AGENTS.md
git commit -m "完成个人 AI 落地网站首页"
git push
```

### 已确认规则

- 网站名称默认使用“will_station专注于AI应用落地”。
- 副标题默认使用“帮中小企业把 AI 真正用到业务里”。
- 第一版不做后台、不做数据库、不做登录、不引入复杂前端框架。
- 网站部署优先使用 GitHub Pages，默认从 `main` 分支根目录发布。

### 后续待办

- 当前微信二维码图片为 `assets/wechat-qr.jpg`，联系模块已直接引用该图片。
- 上线后可增加真实案例、免费诊断表单和企业 AI 落地清单下载入口。
